// static/js/script.js
const apiUrl = 'http://localhost:5000/api';

function escapeHTML(str) {
    // ... (code của hàm escapeHTML)
}

function getInitials(name) {
    // ... (code của hàm getInitials)
}

async function fetchPosts() {
    // ... (code của hàm fetchPosts)
}

function renderPosts(posts) {
    // ... (code của hàm renderPosts)
}
// ... (toàn bộ code JavaScript còn lại từ thẻ <script> trong index.html) ...

window.onload = fetchPosts;
